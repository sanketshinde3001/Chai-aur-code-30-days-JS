// Task 9: Write a program to compare two numbers using >= and <= and log the result to the console.

let a = 5

let b = 10

if (a <= b) {
    console.log(`${b} is bigger or equal than ${a}`)
}

a = 60
b = 60

if (a >= b) {
    console.log(`${a} is bigger or equal than ${b}`)
}